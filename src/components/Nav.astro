---
const links = [
  { href: "/", label: "Accueil" },
  { href: "/mentions-legales", label: "Mentions l̩gales" },
  { href: "/politique-confidentialite", label: "Confidentialit̩" },
  { href: "/contact", label: "Contact" }
];
---
<nav class="mx-auto flex max-w-[var(--container)] items-center justify-between p-4">
  <a href="/" class="flex items-center gap-2 text-xl font-bold">
    <img src="/logo.svg" alt="Optimealone" class="h-8 w-8" />
    <span>Optimealone</span>
  </a>
  <div class="hidden gap-6 md:flex">
    {links.map((l) => <a class="text-slate-600 hover:text-slate-900" href={l.href}>{l.label}</a>)}
  </div>
  <div class="flex items-center gap-3">
    <LoginWidget client:load />
    <SubscribeButton client:load />
  </div>
</nav>

