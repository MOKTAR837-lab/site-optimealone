services:
  web:
    image: ghcr.io/moktar837-lab/site-optimealone:latest
    ports:
      - "80:8080"
    environment:
      SITE: "https://optimealone.example.com"
      BACKEND_URL: "http://backend:8000"
      API_BASE: "/api/v1"
    restart: unless-stopped

